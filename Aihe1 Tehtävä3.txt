#include <future>
#include <vector>
#include <iostream>
#include <thread>
#include <mutex>
#include <numeric>
#include <execution>
#include <stdio.h>
#include <stdlib.h>
using namespace std;

int psum(int N, int start,int end,vector<int> b)
{
	int sum = 0;
	for (int i=start;i<=end;i++)
		sum += b[i];
	return sum;
}

int main() {
	srand((unsigned)time(0));
	vector<int> data = { 1, 2, 3, 4, 100, 6, 7, 8, 9, 10 };
	future<int> fut1 = async(launch::async, psum, 10000000,0,4,data);
	future<int> fut2 = async(launch::async, psum, 10000000,5,9,data);
	int summa = fut1.get() + fut2.get();
	cout << summa;
	return 0;
}