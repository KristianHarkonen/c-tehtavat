#include <iostream>
#include <thread>
#include <vector>
#include <mutex>
using namespace std;
mutex mtx;

void print_message(int number) {
    mtx.lock();
    std::cout << "Hello from thread " << number << " (id=" << this_thread::get_id() << ")\n";
    mtx.unlock();
}

int main()
{
    thread t1(print_message, 1);
    thread t2(print_message, 2);
    thread t3(print_message, 3);
    thread t4(print_message, 4);
    t1.join();
    t2.join();
    t3.join();
    t4.join();
    return 0;
}